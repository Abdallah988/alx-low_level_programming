#include "main.h"
/**
* _isdigit - Check if the character is a digit
* @c: c is a number to be checked
* Return: 1 for digit, 0  for otherwise
*/
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
